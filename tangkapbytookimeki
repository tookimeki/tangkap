-- ==========================================
-- TOOKIMEKI AUTO-FARM SERANGGA v10
-- ==========================================
local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local StarterGui = game:GetService("StarterGui")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

if CoreGui:FindFirstChild("TookimekiAutoFarm") then
    CoreGui.TookimekiAutoFarm:Destroy()
end

-- ==========================================
-- 1. MEMBANGUN UI PREMIUM
-- ==========================================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "TookimekiAutoFarm"
ScreenGui.IgnoreGuiInset = true
ScreenGui.Parent = CoreGui

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 300, 0, 110)
MainFrame.AnchorPoint = Vector2.new(1, 0.5) 
MainFrame.Position = UDim2.new(0.5, 150, 0.5, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(22, 22, 22)
MainFrame.BackgroundTransparency = 0.1
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.ClipsDescendants = true
MainFrame.Parent = ScreenGui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 8)

local Dot = Instance.new("Frame")
Dot.Size = UDim2.new(0, 10, 0, 10)
Dot.Position = UDim2.new(0, 15, 0, 15)
Dot.BackgroundColor3 = Color3.fromRGB(46, 204, 113)
Dot.Parent = MainFrame
Instance.new("UICorner", Dot).CornerRadius = UDim.new(1, 0)

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(0, 200, 0, 20)
Title.Position = UDim2.new(0, 35, 0, 10)
Title.BackgroundTransparency = 1
Title.Text = "Auto-Farm - by tookimeki"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.Gotham
Title.TextSize = 14
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = MainFrame

local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 20, 0, 20)
CloseBtn.Position = UDim2.new(1, -30, 0, 10)
CloseBtn.BackgroundTransparency = 1
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextSize = 16
CloseBtn.Parent = MainFrame

local MinBtn = Instance.new("TextButton")
MinBtn.Size = UDim2.new(0, 20, 0, 20)
MinBtn.Position = UDim2.new(1, -55, 0, 10)
MinBtn.BackgroundTransparency = 1
MinBtn.Text = "-"
MinBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
MinBtn.Font = Enum.Font.GothamBold
MinBtn.TextSize = 20
MinBtn.Parent = MainFrame

local Separator = Instance.new("Frame")
Separator.Size = UDim2.new(1, -30, 0, 1)
Separator.Position = UDim2.new(0, 15, 0, 40)
Separator.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Separator.BackgroundTransparency = 0.8
Separator.BorderSizePixel = 0
Separator.Parent = MainFrame

local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Size = UDim2.new(1, -40, 0, 40)
ToggleBtn.Position = UDim2.new(0, 20, 0, 55)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
ToggleBtn.Text = "AUTO FARM: OFF"
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.Font = Enum.Font.GothamBold
ToggleBtn.TextSize = 14
ToggleBtn.Parent = MainFrame
Instance.new("UICorner", ToggleBtn).CornerRadius = UDim.new(0, 6)

local OpenBtn = Instance.new("ImageButton")
OpenBtn.Size = UDim2.new(0, 0, 0, 0) 
OpenBtn.AnchorPoint = Vector2.new(0.5, 0.5)
OpenBtn.Position = UDim2.new(0.5, 0, 0.5, 0)
OpenBtn.BackgroundColor3 = Color3.fromRGB(22, 22, 22)
OpenBtn.BackgroundTransparency = 0.3 
OpenBtn.Image = "rbxassetid://96528531592202" 
OpenBtn.ImageTransparency = 0.1
OpenBtn.Visible = false
OpenBtn.Parent = ScreenGui
Instance.new("UICorner", OpenBtn).CornerRadius = UDim.new(0, 8)

-- ==========================================
-- 2. SISTEM CUSTOM DRAG
-- ==========================================
local function MakeDraggable(guiObject)
    local dragging = false
    local dragInput, dragStart, startPos

    guiObject.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            if guiObject == MainFrame and ToggleBtn.IsHovered then return end 
            dragging = true
            dragStart = input.Position
            startPos = guiObject.Position 
            
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then dragging = false end
            end)
        end
    end)

    guiObject.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            local targetX = startPos.X.Offset + delta.X
            local targetY = startPos.Y.Offset + delta.Y
            
            local viewport = workspace.CurrentCamera.ViewportSize
            local apX, apY = guiObject.AnchorPoint.X, guiObject.AnchorPoint.Y
            local sizeX, sizeY = guiObject.AbsoluteSize.X, guiObject.AbsoluteSize.Y
            
            local minX = sizeX * apX
            local maxX = viewport.X - sizeX * (1 - apX)
            local minY = sizeY * apY
            local maxY = viewport.Y - sizeY * (1 - apY)
            
            if viewport.X > 0 and viewport.Y > 0 then
                targetX = math.clamp(targetX, minX, maxX)
                targetY = math.clamp(targetY, minY, maxY)
            end
            
            guiObject.Position = UDim2.new(0, targetX, 0, targetY)
        end
    end)
end

MakeDraggable(MainFrame)
MakeDraggable(OpenBtn)

-- ==========================================
-- 3. ANIMASI MINIMIZE & MAXIMIZE
-- ==========================================
local tweenInfo = TweenInfo.new(0.3, Enum.EasingStyle.Quart, Enum.EasingDirection.Out)
local isAnimating = false

local function MaximizeGUI()
    if isAnimating then return end
    isAnimating = true

    local targetX = OpenBtn.Position.X.Offset + 45
    local targetY = OpenBtn.Position.Y.Offset + 50
    local viewport = workspace.CurrentCamera.ViewportSize
    if viewport.X > 0 and viewport.Y > 0 then
        targetX = math.clamp(targetX, 300, viewport.X)
        targetY = math.clamp(targetY, 55, viewport.Y - 55)
    end
    
    TweenService:Create(OpenBtn, tweenInfo, {Size = UDim2.new(0, 0, 0, 0)}):Play()
    task.wait(0.3)
    
    OpenBtn.Visible = false
    MainFrame.Size = UDim2.new(0, 0, 0, 0)
    MainFrame.Position = UDim2.new(0, targetX, 0, targetY)
    MainFrame.Visible = true
    
    TweenService:Create(MainFrame, tweenInfo, {Size = UDim2.new(0, 300, 0, 110)}):Play()
    task.wait(0.3)
    isAnimating = false
end

local function MinimizeGUI()
    if isAnimating then return end
    isAnimating = true
    
    local mousePos = UserInputService:GetMouseLocation()
    local viewport = workspace.CurrentCamera.ViewportSize
    local safeX = math.clamp(mousePos.X, 17.5, viewport.X - 17.5) 
    local safeY = math.clamp(mousePos.Y, 17.5, viewport.Y - 17.5)
    local targetPos = UDim2.new(0, safeX, 0, safeY)
    
    TweenService:Create(MainFrame, tweenInfo, {Size = UDim2.new(0, 0, 0, 0), Position = targetPos}):Play()
    task.wait(0.3)
    
    MainFrame.Visible = false
    OpenBtn.Position = targetPos
    OpenBtn.Size = UDim2.new(0, 0, 0, 0)
    OpenBtn.Visible = true
    
    TweenService:Create(OpenBtn, tweenInfo, {Size = UDim2.new(0, 35, 0, 35)}):Play()
    task.wait(0.3)
    isAnimating = false
end

local clickStart, clickPos
OpenBtn.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        clickStart = os.clock()
        clickPos = input.Position
    end
end)
OpenBtn.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        if clickStart and clickPos then
            local delta = (input.Position - clickPos).Magnitude
            if delta < 5 and (os.clock() - clickStart) < 0.5 then
                task.spawn(MaximizeGUI)
            end
            clickStart, clickPos = nil, nil
        end
    end
end)

MinBtn.MouseButton1Click:Connect(function() task.spawn(MinimizeGUI) end)

-- ==========================================
-- 4. LOGIKA AUTO-FARM (VIRTUAL LOCK LOGIC)
-- ==========================================
local autoFarmActive = false
local loopRunning = true

-- Variabel Pengunci
local isWaitingForMinigame = false
local lastLockTime = 0

local function VirtualClick(button)
    if not button then return end
    if getconnections then
        for _, connection in pairs(getconnections(button.MouseButton1Click)) do connection:Fire() end
        for _, connection in pairs(getconnections(button.MouseButton1Down)) do connection:Fire() end
        for _, connection in pairs(getconnections(button.Activated)) do connection:Fire() end
    end
end

-- LOOP UTAMA
task.spawn(function()
    while loopRunning do
        task.wait(0.1) 
        
        if autoFarmActive then
            -- A. EQUIP JARING
            local character = LocalPlayer.Character
            if character then
                local humanoid = character:FindFirstChildOfClass("Humanoid")
                local jaring = LocalPlayer.Backpack:FindFirstChild("Jaring") or character:FindFirstChild("Jaring")
                
                if jaring and jaring.Parent ~= character then
                    if humanoid then humanoid:EquipTool(jaring) end
                end
            end
            
            -- B. SCAN LAYAR
            local insectFound = nil
            local catchBtnFound = nil
            
            for _, gui in pairs(PlayerGui:GetChildren()) do
                if gui:IsA("ScreenGui") and gui.Enabled then
                    
                    -- Cari Serangga
                    if not insectFound then
                        local insect = gui:FindFirstChild("Dragonfly", true) or 
                                       gui:FindFirstChild("Butterfly", true) or 
                                       gui:FindFirstChild("Frog", true)
                        if insect and insect:IsA("GuiButton") and insect.Visible then
                            insectFound = insect
                        end
                    end
                    
                    -- Cari Tombol Hijau Jaring
                    if not catchBtnFound then
                        local btn = gui:FindFirstChild("CatchButton", true)
                        if btn and btn:IsA("GuiButton") and btn.Visible then
                            catchBtnFound = btn
                        end
                    end
                    
                end
            end
            
            -- C. LOGIKA KUNCI VIRTUAL
            if insectFound then
                -- Jika serangga muncul, klik!
                VirtualClick(insectFound)
                -- Langsung BUKA KUNCI karena proses tangkap sudah selesai
                isWaitingForMinigame = false 
                
            elseif catchBtnFound then
                -- Jika tombol hijau muncul, cek apakah kuncinya terbuka
                if not isWaitingForMinigame then
                    VirtualClick(catchBtnFound)
                    -- KUNCI SISTEM agar tidak diklik lagi saat tombolnya menggelap
                    isWaitingForMinigame = true 
                    lastLockTime = os.clock()
                else
                    -- Failsafe: Jika sistem terkunci lebih dari 4 detik (misal serangga gagal muncul),
                    -- buka kembali kuncinya secara paksa agar tidak macet selamanya.
                    if os.clock() - lastLockTime > 4 then
                        isWaitingForMinigame = false
                    end
                end
            end
            
        end
    end
end)

ToggleBtn.MouseButton1Click:Connect(function()
    autoFarmActive = not autoFarmActive 
    if autoFarmActive then
        ToggleBtn.Text = "AUTO FARM: ON"
        ToggleBtn.BackgroundColor3 = Color3.fromRGB(46, 204, 113) 
        isWaitingForMinigame = false -- Reset kunci saat dinyalakan
    else
        ToggleBtn.Text = "AUTO FARM: OFF"
        ToggleBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50) 
        local character = LocalPlayer.Character
        if character then
            local humanoid = character:FindFirstChildOfClass("Humanoid")
            if humanoid then humanoid:UnequipTools() end
        end
    end
end)

CloseBtn.MouseButton1Click:Connect(function()
    loopRunning = false 
    autoFarmActive = false
    pcall(function()
        StarterGui:SetCore("SendNotification", {
            Title = "Auto-Farm",
            Text = "Script ditutup.",
            Icon = "rbxassetid://96528531592202",
            Duration = 3,
        })
    end)
    ScreenGui:Destroy()
end)

print("Tookimeki Auto-Farm v10 (Perfect Sync) Aktif!")
